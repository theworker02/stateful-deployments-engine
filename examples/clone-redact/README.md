# Clone + redaction

Safe archive clone with text-only redaction/tokenization hooks.

```bash
go run ./examples/clone-redact
```

Binary blobs are left unchanged — SDE does not invent binary anonymization.
